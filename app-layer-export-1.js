const clipLibrary = [
  ["aic-102777-abigail-chesebrough-mrs-alexander-grant", "Abigail Chesebrough (Mrs. Alexander Grant)", "painting", "assets/cutout-clippings/aic-102777-abigail-chesebrough-mrs-alexander-grant.png"],
  ["aic-10410-tazza", "Tazza", "vessel for serving foo", "assets/cutout-clippings/aic-10410-tazza.png"],
  ["aic-106279-garniture-for-field-and-foot-tourney-at-the-barriers", "Garniture for Field and Foot Tourney at th", "suit", "assets/cutout-clippings/aic-106279-garniture-for-field-and-foot-tourney-at-the-barriers.png"],
  ["aic-111-martin-vanden-bogaert-desjardins", "Martin Vanden Bogaert Desjardins", "engraving", "assets/cutout-clippings/aic-111-martin-vanden-bogaert-desjardins.png"],
  ["aic-11344-sadie-pfeifer-a-cotton-mill-spinner-lancaster-south-", "Sadie Pfeifer, a Cotton Mill Spinner, Lanc", "gelatin silver (develo", "assets/cutout-clippings/aic-11344-sadie-pfeifer-a-cotton-mill-spinner-lancaster-south-.png"],
  ["aic-120154-still-life-strawberries-nuts-c", "Still Life—Strawberries, Nuts, &c.", "painting", "assets/cutout-clippings/aic-120154-still-life-strawberries-nuts-c.png"],
  ["aic-126336-captain-verschoyle-grenadier-guards-an-early-photogr", "Captain Verschoyle, Grenadier Guards (an E", "albumen silver print", "assets/cutout-clippings/aic-126336-captain-verschoyle-grenadier-guards-an-early-photogr.png"],
  ["aic-13598-aristide-bruant", "Aristide Bruant", "lithograph", "assets/cutout-clippings/aic-13598-aristide-bruant.png"],
  ["aic-143683-handkerchief", "Handkerchief", "textile", "assets/cutout-clippings/aic-143683-handkerchief.png"],
  ["aic-156596-fruit-piece", "Fruit Piece", "painting", "assets/cutout-clippings/aic-156596-fruit-piece.png"],
  ["aic-160160-dance-skirt-photographed-with-apron", "Dance Skirt (photographed with Apron)", "leather/animal skin", "assets/cutout-clippings/aic-160160-dance-skirt-photographed-with-apron.png"],
  ["aic-19-view-of-ponte-lugano-on-the-anio-from-views-of-rome", "View of Ponte Lugano on the Anio, from Vie", "etching", "assets/cutout-clippings/aic-19-view-of-ponte-lugano-on-the-anio-from-views-of-rome.png"],
  ["aic-19333-kitchen-still-life", "Kitchen Still Life", "oil on canvas", "assets/cutout-clippings/aic-19333-kitchen-still-life.png"],
  ["aic-20-it-rocks-but-is-not-sunk", "It Rocks but is Not Sunk", "etching", "assets/cutout-clippings/aic-20-it-rocks-but-is-not-sunk.png"],
  ["aic-213791-new-patented-umbrella-with-an-improved-spring-system", "New patented umbrella, with an improved sp", "lithograph", "assets/cutout-clippings/aic-213791-new-patented-umbrella-with-an-improved-spring-system.png"],
  ["aic-215528-street-advertising", "Street Advertising", "photograph", "assets/cutout-clippings/aic-215528-street-advertising.png"],
  ["aic-234687-tureen", "Tureen", "vessel for serving foo", "assets/cutout-clippings/aic-234687-tureen.png"],
  ["aic-24-portrait-of-edouard-mol", "Portrait of Edouard Molé", "engraving", "assets/cutout-clippings/aic-24-portrait-of-edouard-mol.png"],
  ["aic-24320-collar-made-from-border", "Collar (Made from Border)", "textile", "assets/cutout-clippings/aic-24320-collar-made-from-border.png"],
  ["aic-25865-the-herring-net", "The Herring Net", "painting", "assets/cutout-clippings/aic-25865-the-herring-net.png"],
  ["aic-27-remembrance-of-italy", "Remembrance of Italy", "etching", "assets/cutout-clippings/aic-27-remembrance-of-italy.png"],
  ["aic-27047-a-scene-in-york-york-minster-from-lop-lane", "\"A Scene in York\" - York Minster from Lop ", "salted paper print", "assets/cutout-clippings/aic-27047-a-scene-in-york-york-minster-from-lop-lane.png"],
  ["aic-31707-general-george-armstrong-custer", "General George Armstrong Custer", "carte-de-visite (album", "assets/cutout-clippings/aic-31707-general-george-armstrong-custer.png"],
  ["aic-34078-food-container", "Food container", "vessel", "assets/cutout-clippings/aic-34078-food-container.png"],
  ["aic-37423-covered-food-container", "Covered Food Container", "vessel", "assets/cutout-clippings/aic-37423-covered-food-container.png"],
  ["aic-4749-mrs-george-swinton-elizabeth-ebsworth", "Mrs. George Swinton (Elizabeth Ebsworth)", "painting", "assets/cutout-clippings/aic-4749-mrs-george-swinton-elizabeth-ebsworth.png"],
  ["aic-55833-covered-food-container", "Covered Food Container", "vessel", "assets/cutout-clippings/aic-55833-covered-food-container.png"],
  ["aic-60294-new-york-street", "New York Street", "oil on canvas", "assets/cutout-clippings/aic-60294-new-york-street.png"],
  ["aic-65-fishing-craft-near-the-cliffs-at-collioure", "Fishing Craft near the Cliffs at Collioure", "etching", "assets/cutout-clippings/aic-65-fishing-craft-near-the-cliffs-at-collioure.png"],
  ["aic-66489-georgia-o-keeffe-torso", "Georgia O'Keeffe—Torso", "gelatin silver (develo", "assets/cutout-clippings/aic-66489-georgia-o-keeffe-torso.png"],
  ["aic-7934-platter", "Platter", "vessel for serving foo", "assets/cutout-clippings/aic-7934-platter.png"],
  ["aic-81561-street-in-moret", "Street in Moret", "oil on canvas", "assets/cutout-clippings/aic-81561-street-in-moret.png"],
  ["aic-88629-reine-de-joie", "Reine de Joie", "lithograph", "assets/cutout-clippings/aic-88629-reine-de-joie.png"],
  ["aic-9-interior-of-st-mark-s-venice", "Interior of St. Mark's, Venice", "painting", "assets/cutout-clippings/aic-9-interior-of-st-mark-s-venice.png"],
  ["aic-97907-a-mexican-vaquero", "A Mexican Vaquero", "painting", "assets/cutout-clippings/aic-97907-a-mexican-vaquero.png"],
  ["aic-97912-the-mexican-major", "The Mexican Major", "painting", "assets/cutout-clippings/aic-97912-the-mexican-major.png"],
  ["met-13722-memorial-to-salome-and-moses-brown", "Memorial to Salome and Moses Brown", "The American Wing", "assets/cutout-clippings/met-13722-memorial-to-salome-and-moses-brown.png"],
  ["met-14298-clock", "Clock", "The American Wing", "assets/cutout-clippings/met-14298-clock.png"],
  ["met-20519-brooch", "Brooch", "The American Wing", "assets/cutout-clippings/met-20519-brooch.png"],
  ["met-20522-ring", "Ring", "The American Wing", "assets/cutout-clippings/met-20522-ring.png"],
  ["met-39-advertisement-for-norwich-stone-ware-factory", "Advertisement for Norwich Stone Ware Facto", "The American Wing", "assets/cutout-clippings/met-39-advertisement-for-norwich-stone-ware-factory.png"],
  ["met-433565-poster-for-raynaud-fr-res", "Poster for Raynaud Frères", "Drawings and Prints", "assets/cutout-clippings/met-433565-poster-for-raynaud-fr-res.png"],
  ["met-548246-viscera-figure-with-human-head-imseti", "Viscera figure with human head (Imseti)", "Egyptian Art", "assets/cutout-clippings/met-548246-viscera-figure-with-human-head-imseti.png"],
  ["met-550814-viscera-figure-with-human-head-imseti", "Viscera figure with human head (Imseti)", "Egyptian Art", "assets/cutout-clippings/met-550814-viscera-figure-with-human-head-imseti.png"],
  ["met-570751-package-of-disorganized-animal-remains-adorned-with-", "Package of disorganized animal remains ado", "Egyptian Art", "assets/cutout-clippings/met-570751-package-of-disorganized-animal-remains-adorned-with-.png"],
  ["met-577416-viscera-figure-with-baboon-head-hapy", "Viscera figure with baboon head (Hapy)", "Egyptian Art", "assets/cutout-clippings/met-577416-viscera-figure-with-baboon-head-hapy.png"],
  ["met-577417-viscera-figure-with-jackal-head-duamutef", "Viscera figure with jackal head (Duamutef)", "Egyptian Art", "assets/cutout-clippings/met-577417-viscera-figure-with-jackal-head-duamutef.png"],
  ["met-577418-viscera-figure-with-falcoln-head-qebehsenuef", "Viscera figure with falcoln head (Qebehsen", "Egyptian Art", "assets/cutout-clippings/met-577418-viscera-figure-with-falcoln-head-qebehsenuef.png"],
  ["met-577422-viscera-figure-with-baboon-head-hapy", "Viscera figure with baboon head (Hapy)", "Egyptian Art", "assets/cutout-clippings/met-577422-viscera-figure-with-baboon-head-hapy.png"],
  ["met-577423-viscera-figure-with-jackal-head-duamutef", "Viscera figure with jackal head (Duamutef)", "Egyptian Art", "assets/cutout-clippings/met-577423-viscera-figure-with-jackal-head-duamutef.png"],
  ["met-577428-viscera-figure-with-baboon-head-hapy", "Viscera figure with baboon head (Hapy)", "Egyptian Art", "assets/cutout-clippings/met-577428-viscera-figure-with-baboon-head-hapy.png"],
  ["met-577430-viscera-figure-with-falcon-head-qebehsenuef", "Viscera figure with falcon head (Qebehsenu", "Egyptian Art", "assets/cutout-clippings/met-577430-viscera-figure-with-falcon-head-qebehsenuef.png"],
  ["met-786300-buzz-aldrin-on-the-moon-with-components-of-the-early", "Buzz Aldrin on the Moon with Components of", "Photographs", "assets/cutout-clippings/met-786300-buzz-aldrin-on-the-moon-with-components-of-the-early.png"],
  ["aic-136740-funerary-mask", "Funerary Mask", "Funerary Object", "assets/cutout-clippings/aic-136740-funerary-mask.png"],
  ["aic-181740-male-face-mask-chihongo", "Male Face Mask (Chihongo)", "Mask", "assets/cutout-clippings/aic-181740-male-face-mask-chihongo.png"],
  ["aic-212967-face", "Face", "Sculpture", "assets/cutout-clippings/aic-212967-face.png"],
  ["aic-576-fan", "Fan", "Costume and Accessories", "assets/cutout-clippings/aic-576-fan.png"],
  ["aic-245-oinochoe-pitcher", "Oinochoe (Pitcher)", "Vessel", "assets/cutout-clippings/aic-245-oinochoe-pitcher.png"],
  ["aic-38666-the-water-fan", "The Water Fan", "Drawing and Watercolor", "assets/cutout-clippings/aic-38666-the-water-fan.png"],
  ["aic-15906-man-s-boots", "Man's Boots", "Costume and Accessories", "assets/cutout-clippings/aic-15906-man-s-boots.png"],
  ["aic-15910-woman-s-shoes", "Woman's Shoes", "Costume and Accessories", "assets/cutout-clippings/aic-15910-woman-s-shoes.png"],
  ["aic-19432-qabqabs-bath-clogs", "Qabqabs (Bath Clogs)", "Textile", "assets/cutout-clippings/aic-19432-qabqabs-bath-clogs.png"],
  ["aic-19423-folding-shoe-measure", "Folding Shoe Measure", "Textile", "assets/cutout-clippings/aic-19423-folding-shoe-measure.png"],
  ["aic-27281-madam-pompadour", "Madam Pompadour", "Painting", "assets/cutout-clippings/aic-27281-madam-pompadour.png"],
  ["aic-135-jar-with-tubular-handles-peonies-endless-knot-pendant-ball", "Jar with Tubular Handles, Peonies, “Endless Knot,” Pendant Balls, and ", "Vessel", "assets/cutout-clippings/aic-135-jar-with-tubular-handles-peonies-endless-knot-pendant-ball.png"],
  ["aic-68769-vase-vase-trusque-rouleau", "Vase (Vase étrusque à rouleau)", "Ceramics", "assets/cutout-clippings/aic-68769-vase-vase-trusque-rouleau.png"],
  ["aic-151387-vase-with-cover", "Vase with Cover", "Ceramics", "assets/cutout-clippings/aic-151387-vase-with-cover.png"],
  ["aic-10896-vase-vase-oreilles", "Vase (Vase à Oreilles)", "Ceramics", "assets/cutout-clippings/aic-10896-vase-vase-oreilles.png"],
  ["aic-6681-flower-vase-one-of-a-pair", "Flower Vase (one of a pair)", "Ceramics", "assets/cutout-clippings/aic-6681-flower-vase-one-of-a-pair.png"],
  ["aic-12229-dragon-handled-amphora", "Dragon-Handled Amphora 三彩双龙耳瓶", "Decorative Arts", "assets/cutout-clippings/aic-12229-dragon-handled-amphora.png"],
  ["aic-33053-rocking-chair", "Rocking Chair", "Furniture", "assets/cutout-clippings/aic-33053-rocking-chair.png"],
  ["aic-121781-chair", "Chair", "Furniture", "assets/cutout-clippings/aic-121781-chair.png"],
  ["aic-189715-king-vulture", "King Vulture", "Ceramics", "assets/cutout-clippings/aic-189715-king-vulture.png"],
  ["aic-1601-panel-judith-with-the-head-of-holofernes-possibly-from-an", "Panel (Judith with the head of Holofernes, Possibly from an Altar Fron", "Textile", "assets/cutout-clippings/aic-1601-panel-judith-with-the-head-of-holofernes-possibly-from-an.png"],
  ["aic-181-amphora-storage-jar", "Amphora (Storage Jar)", "Vessel", "assets/cutout-clippings/aic-181-amphora-storage-jar.png"],
  ["aic-198337-amphora-storage-jar", "Amphora (Storage Jar)", "Vessel", "assets/cutout-clippings/aic-198337-amphora-storage-jar.png"],
  ["aic-885-a-mounted-officer", "A Mounted Officer", "Painting", "assets/cutout-clippings/aic-885-a-mounted-officer.png"],
  ["aic-81533-the-races-at-longchamp", "The Races at Longchamp", "Painting", "assets/cutout-clippings/aic-81533-the-races-at-longchamp.png"],
  ["aic-145840-cheval-turk", "Cheval Turk", "Sculpture", "assets/cutout-clippings/aic-145840-cheval-turk.png"],
  ["aic-181145-cup-in-the-form-of-a-horse-and-rider", "Cup in the Form of a Horse and Rider", "Metalwork", "assets/cutout-clippings/aic-181145-cup-in-the-form-of-a-horse-and-rider.png"],
  ["aic-106327-pair-of-rowel-spurs", "Pair of Rowel Spurs", "Armor", "assets/cutout-clippings/aic-106327-pair-of-rowel-spurs.png"],
  ["aic-10083-dupondius-coin-portraying-germanicus-caesar", "Dupondius (Coin) Portraying Germanicus Caesar", "Coin", "assets/cutout-clippings/aic-10083-dupondius-coin-portraying-germanicus-caesar.png"],
  ["aic-158-pelike-storage-jar", "Pelike (Storage Jar)", "Vessel", "assets/cutout-clippings/aic-158-pelike-storage-jar.png"],
  ["aic-40724-the-offering-of-the-boar-s-head-from-the-story-of-meleager", "The Offering of the Boar's Head from The Story of Meleager and Atalant", "Textile", "assets/cutout-clippings/aic-40724-the-offering-of-the-boar-s-head-from-the-story-of-meleager.png"],
  ["aic-896-the-road-to-market", "The Road to Market", "Painting", "assets/cutout-clippings/aic-896-the-road-to-market.png"],
  ["aic-9028-torso-from-a-doll", "Torso From a Doll", "Sculpture", "assets/cutout-clippings/aic-9028-torso-from-a-doll.png"],
  ["aic-103045-dolls", "Dolls", "Decorative Arts", "assets/cutout-clippings/aic-103045-dolls.png"],
  ["aic-103046-dolls", "Dolls", "Decorative Arts", "assets/cutout-clippings/aic-103046-dolls.png"],
].map(([slug,label,meta,src]) => ({ slug, label, meta, src }));

const prompts = [
  'Gas station prophecy after summer practice',
  'A haunted coupon for the best day of July',
  'Local mascot seen behind the corner store',
  'Orange drink commercial from a lost VHS tape',
  'Streetwear receipt found in a mall fountain',
  'Public access warning before the big game',
  'A flyer for a party nobody remembers attending',
  'The menu board at the end of the world',
  'A mall kiosk sells you your childhood back',
  'Team photo for a sport nobody understands',
  'A lost-and-found table at the county fair',
  'The official poster for a dream you forgot',
  'A souvenir from a place that closed in 1998',
  'Evidence from a very small neighborhood mystery',
  'A weather report for indoor rain',
  'A birthday card from the future suburbs',
  'The last page of a cursed school yearbook',
  'A product catalog for impossible furniture',
  'A postcard mailed from the food court',
  'A warning label for being too sentimental',
  'The halftime show for a haunted little league game',
  'A coupon book for bad decisions',
  'A museum label for something found under the bed',
  'The cover of a zine sold at a gas station counter',
  'A roadside attraction nobody can prove existed',
  'An ad for a summer camp run by ghosts',
  'The opening scene of a public access soap opera',
  'A trophy case after everyone went home',
  'A map to the best vending machine in town',
  'The mascot reveal went beautifully wrong',
  'A classified ad for a lost feeling',
  'A vacation brochure for the edge of town',
  'A shrine to the last slice of pizza',
  'The official seal of a secret basement club',
  'A receipt from the universe',
  'A field guide to parking lot creatures',
  'The poster for a movie filmed inside a thrift store',
  'A menu for the sleepover after midnight',
  'A certificate of participation from the apocalypse',
  'The window display of a magic convenience store',
  'A campaign poster for class president of nowhere',
  'The instruction manual for becoming local legend',
  'A family photo from an alternate timeline',
  'A parade float for a forgotten holiday',
  'A ransom note made by a very polite ghost',
  'The souvenir cup from a doomed theme park',
  'A mixtape cover for driving around with no destination',
  'The front page of a newspaper printed tomorrow',
  'A scrapbook page from the strangest day of summer',
  'The logo for a club that meets behind the grocery store'
];

const tray = document.getElementById('clipTray');
const artboard = document.getElementById('artboard');
const usedCount = document.getElementById('usedCount');
const limitCount = document.getElementById('limitCount');
const promptEl = document.getElementById('dailyPrompt');
const LIMIT = 15;
const DAILY_PACK_VERSION = '2026-05-13-reroll-2';
let z = 10;
let selected = null;
let activeDrag = null;
let boardPieces = [];
let todayPack = [];
let historyStack = [];
let restoring = false;

limitCount.textContent = LIMIT;


function placedClipCount(){
  return boardPieces.filter(p => p.dataset.kind !== 'text').length;
}

function serializePiece(p){
  return {
    kind: p.dataset.kind || 'image',
    id: p.dataset.id,
    slug: p.dataset.slug || '',
    label: p.dataset.label || '',
    src: p.dataset.src || '',
    text: p.dataset.text || '',
    x: p.dataset.x, y: p.dataset.y,
    w: p.dataset.w, h: p.dataset.h,
    rot: p.dataset.rot, scale: p.dataset.scale,
    locked: p.dataset.locked || '0',
    zIndex: p.style.zIndex || '10'
  };
}

function pushHistory(){
  if(restoring) return;
  historyStack.push(boardPieces.map(serializePiece));
  if(historyStack.length > 40) historyStack.shift();
}

function createTextPieceFromData(data){
  const piece = document.createElement('div');
  piece.className = 'piece text-piece';
  piece.dataset.kind = 'text';
  piece.dataset.id = data.id || (crypto.randomUUID ? crypto.randomUUID() : String(Date.now()+Math.random()));
  piece.dataset.text = data.text || 'LOCAL ONLY';
  piece.dataset.x = data.x || '90'; piece.dataset.y = data.y || '120';
  piece.dataset.w = data.w || '210'; piece.dataset.h = data.h || '72';
  piece.dataset.rot = data.rot || String(randomPlacementRotation());
  piece.dataset.scale = data.scale || '1';
  piece.dataset.locked = data.locked || '0';
  piece.classList.toggle('locked', piece.dataset.locked === '1');
  piece.style.width = `${piece.dataset.w}px`; piece.style.height = `${piece.dataset.h}px`;
  piece.style.zIndex = data.zIndex || String(++z);
  piece.innerHTML = `<span class="text-scrap-label"></span><span class="lock-badge" aria-hidden="true">LOCKED</span><span class="rotate-handle" data-rotate title="Drag to rotate">↻</span>`;
  piece.querySelector('.text-scrap-label').textContent = piece.dataset.text;
  artboard.appendChild(piece);
  wirePiece(piece);
  applyTransform(piece);
  return piece;
}

function createImagePieceFromData(data){
  const clip = { slug:data.slug, label:data.label, src:data.src };
  const piece = buildImagePiece(clip);
  ['id','x','y','w','h','rot','scale','slug','label','src','locked'].forEach(k => { if(data[k] !== undefined) piece.dataset[k] = data[k]; });
  piece.classList.toggle('locked', piece.dataset.locked === '1');
  piece.style.width = `${piece.dataset.w}px`; piece.style.height = `${piece.dataset.h}px`;
  piece.style.zIndex = data.zIndex || String(++z);
  artboard.appendChild(piece);
  wirePiece(piece);
  applyTransform(piece);
  return piece;
}

function restoreSnapshot(snapshot){
  restoring = true;
  boardPieces.forEach(p => p.remove());
  boardPieces = [];
  selected = null;
  snapshot.forEach(data => boardPieces.push(data.kind === 'text' ? createTextPieceFromData(data) : createImagePieceFromData(data)));
  normalizeLayers([...boardPieces].sort((a,b) => Number(a.style.zIndex||0) - Number(b.style.zIndex||0)));
  syncTrayUsed();
  updateLockButton();
  updateCount();
  restoring = false;
}

function undoLast(){
  const snap = historyStack.pop();
  if(!snap) return;
  restoreSnapshot(snap);
}

function syncTrayUsed(){
  const used = new Set(boardPieces.filter(p => p.dataset.kind !== 'text').map(p => p.dataset.slug));
  tray.querySelectorAll('.clip').forEach(btn => {
    const isUsed = used.has(btn.dataset.slug);
    btn.classList.toggle('used', isUsed);
    btn.disabled = isUsed || placedClipCount() >= LIMIT;
  });
}

function todayKey(offset = 0){
  // Public daily challenge: use one shared Pulp day for everyone.
  // Packets rotate at midnight America/New_York instead of each visitor's local timezone.
  const now = new Date();
  const eastern = new Date(now.toLocaleString('en-US', { timeZone: 'America/New_York' }));
  eastern.setDate(eastern.getDate() + offset);
  return `${eastern.getFullYear()}-${String(eastern.getMonth()+1).padStart(2,'0')}-${String(eastern.getDate()).padStart(2,'0')}`;
}

function hashString(str){
  let h = 2166136261;
  for(let i=0;i<str.length;i++){ h ^= str.charCodeAt(i); h = Math.imul(h, 16777619); }
  return h >>> 0;
}

function seededShuffle(arr, seed){
  const out = [...arr];
  let s = seed >>> 0;
  const rand = () => { s = Math.imul(1664525, s) + 1013904223 >>> 0; return s / 4294967296; };
  for(let i=out.length-1;i>0;i--){ const j = Math.floor(rand() * (i+1)); [out[i],out[j]] = [out[j],out[i]]; }
  return out;
}

async function dailyPack(){
  const key = todayKey();
  promptEl.textContent = prompts[hashString(key) % prompts.length];
  try {
    const res = await fetch(`daily-packs/${key}.json?v=${encodeURIComponent(`${key}-${DAILY_PACK_VERSION}`)}`, { cache: 'no-store' });
    if(res.ok){
      const pack = await res.json();
      if(Array.isArray(pack.clippings) && pack.clippings.length >= LIMIT){
        return pack.clippings.slice(0, LIMIT);
      }
    }
  } catch (err) {
    console.warn('Daily pack unavailable', err);
  }
  return [];
}

function paintTray(pack){
  tray.innerHTML = '';
  const used = new Set(boardPieces.filter(p => p.dataset.kind !== 'text').map(p => p.dataset.slug));
  pack.forEach((clip, i) => {
    const btn = document.createElement('button');
    btn.type = 'button';
    btn.className = 'clip image-clip';
    btn.dataset.slug = clip.slug;
    btn.dataset.label = clip.label;
    btn.innerHTML = `<span class="clip-number">${String(i+1).padStart(2,'0')}</span><img src="${clip.src}" alt="${clip.label}" draggable="false"><span class="label">${clip.label}</span><span class="meta">${clip.meta}</span>`;
    if(used.has(clip.slug)) btn.classList.add('used');
    btn.addEventListener('click', () => addPiece(clip, btn));
    tray.appendChild(btn);
  });
  updateCount();
}

async function renderTray(){
  tray.innerHTML = '<p class="tray-loading">Loading today’s clippings…</p>';
  todayPack = await dailyPack();
  if(!todayPack.length){
    tray.innerHTML = '<p class="tray-loading">Today’s clippings did not load. Refresh once, and if this keeps happening we’ll rebuild the pack.</p>';
    return;
  }
  paintTray(todayPack);
}

function shuffleTray(){
  if(!todayPack.length) return;
  const seed = hashString(`${todayKey()}-${Date.now()}-${Math.random()}`);
  todayPack = seededShuffle(todayPack, seed);
  paintTray(todayPack);
}

function seedStarterPieces(){
  // Show the game immediately with starter scraps sized to the current board.
  const mobile = window.matchMedia('(max-width: 720px)').matches;
  const count = mobile ? 3 : 5;
  const bw = artboard.clientWidth || 360;
  const bh = artboard.clientHeight || bw;
  const layouts = mobile
    ? [
        [.10, .12, 0.58, -8, .48, .34],
        [.43, .26, 0.52, 7, .40, .44],
        [.19, .54, 0.48, -3, .50, .32]
      ]
    : [
        [.10, .14, 0.82, -8, .33, .26],
        [.38, .22, 0.70, 5, .27, .35],
        [.62, .10, 0.64, -12, .36, .23],
        [.20, .52, 0.58, 9, .22, .32],
        [.54, .56, 0.72, 2, .30, .27]
      ];
  todayPack.slice(0, count).forEach((clip, i) => {
    const btn = [...tray.children].find(el => el.dataset.slug === clip.slug);
    if(btn && !btn.classList.contains('used')) addPiece(clip, btn);
    const piece = boardPieces[boardPieces.length - 1];
    const [px, py, scale, rot, pw, ph] = layouts[i];
    if(piece){
      piece.dataset.w = String(Math.round(Math.max(120, bw * pw)));
      piece.dataset.h = String(Math.round(Math.max(105, bh * ph)));
      piece.dataset.x = String(Math.round(Math.min(bw - 70, bw * px)));
      piece.dataset.y = String(Math.round(Math.min(bh - 70, bh * py)));
      piece.dataset.rot = String(rot);
      piece.dataset.scale = String(scale);
      piece.style.width = `${piece.dataset.w}px`;
      piece.style.height = `${piece.dataset.h}px`;
      applyTransform(piece);
    }
  });
}

function updateCount(){
  usedCount.textContent = placedClipCount();
  const full = placedClipCount() >= LIMIT;
  tray.querySelectorAll('.clip').forEach(c => { if(!c.classList.contains('used')) c.disabled = full; });
}

function makeTornPoints(){
  // Build a unique jagged silhouette around the image edge.
  // Points are percentages so CSS and canvas export can share the same shape.
  const pts = [];
  const steps = 5 + Math.floor(Math.random() * 4);
  const jitter = () => 1.5 + Math.random() * 8;
  for(let i=0;i<=steps;i++) pts.push([i/steps*100, jitter()]);
  for(let i=1;i<=steps;i++) pts.push([100-jitter(), i/steps*100]);
  for(let i=steps-1;i>=0;i--) pts.push([i/steps*100, 100-jitter()]);
  for(let i=steps-1;i>=1;i--) pts.push([jitter(), i/steps*100]);
  return pts.map(([x,y]) => `${x.toFixed(1)}% ${y.toFixed(1)}%`).join(', ');
}

function expandedPolygon(poly, amount = 2.4){
  return poly.replace(/([0-9.]+)% ([0-9.]+)%/g, (_, xs, ys) => {
    let x = Number(xs), y = Number(ys);
    x = x < 50 ? Math.max(0, x - amount) : Math.min(100, x + amount);
    y = y < 50 ? Math.max(0, y - amount) : Math.min(100, y + amount);
    return `${x.toFixed(1)}% ${y.toFixed(1)}%`;
  });
}

function paperTone(){
  return ['#fffdf4','#fff3cf','#f5dfb3','#f8ead0','#fff8e8','#eee1c6','#f7c9d8','#cfe7d0','#d6e6ff','#f2b66d','#e8e1d1','#f9f871'][Math.floor(Math.random()*12)];
}

function edgeTone(){
  return ['#d7c49b','#ead8b1','#cdbb95','#f1dbac','#bfae8a'][Math.floor(Math.random()*5)];
}

function backingPolygon(){
  // Separate torn backing shape: deliberately chunkier than the image crop.
  const pts = [];
  const steps = 4 + Math.floor(Math.random() * 5);
  const jitter = () => -6 + Math.random() * 14;
  for(let i=0;i<=steps;i++) pts.push([i/steps*100, jitter()]);
  for(let i=1;i<=steps;i++) pts.push([100+jitter(), i/steps*100]);
  for(let i=steps-1;i>=0;i--) pts.push([i/steps*100, 100+jitter()]);
  for(let i=steps-1;i>=1;i--) pts.push([jitter(), i/steps*100]);
  return pts.map(([x,y]) => `${Math.max(-8, Math.min(108,x)).toFixed(1)}% ${Math.max(-8, Math.min(108,y)).toFixed(1)}%`).join(', ');
}

function randomPlacementRotation(){
  // Keep new PNG cutouts feeling handmade: varied, but not upside-down.
  // Avoid the dead-looking near-zero rotations that made pieces feel repeated.
  const magnitude = 4 + Math.random() * 24;
  return Math.round((Math.random() < 0.5 ? -1 : 1) * magnitude);
}

function buildImagePiece(clip){
  const piece = document.createElement('div');
  const id = crypto.randomUUID ? crypto.randomUUID() : String(Date.now()+Math.random());
  const scale = 0.55 + Math.random() * 0.38;
  const rot = randomPlacementRotation();
  const x = 60 + Math.random() * Math.max(80, artboard.clientWidth - 220);
  const y = 80 + Math.random() * Math.max(80, artboard.clientHeight - 240);
  piece.className = 'piece image-piece';
  piece.dataset.kind = 'image';
  piece.dataset.id = id;
  piece.dataset.x = x;
  piece.dataset.y = y;
  piece.dataset.rot = rot;
  piece.dataset.scale = scale.toFixed(2);
  piece.dataset.locked = '0';
  const sizeRoll = Math.random();
  piece.dataset.w = String(Math.round(sizeRoll < 0.25 ? 120 + Math.random() * 90 : sizeRoll > 0.78 ? 300 + Math.random() * 170 : 185 + Math.random() * 150));
  piece.dataset.h = String(Math.round(sizeRoll < 0.25 ? 110 + Math.random() * 100 : sizeRoll > 0.78 ? 240 + Math.random() * 190 : 150 + Math.random() * 175));
  piece.dataset.cut = 'clean';
  piece.dataset.cutPoly = '0% 0%, 100% 0%, 100% 100%, 0% 100%';
  piece.dataset.edgePoly = piece.dataset.cutPoly;
  piece.dataset.paper = '#fffdf4';
  piece.dataset.edge = '#fffdf4';
  piece.dataset.padTop = '0';
  piece.dataset.padRight = '0';
  piece.dataset.padBottom = '0';
  piece.dataset.padLeft = '0';
  piece.dataset.backingType = 'none';
  piece.dataset.edgeOffsetX = '0';
  piece.dataset.edgeOffsetY = '0';
  piece.dataset.edgeRot = '0deg';
  piece.dataset.tape = '0';
  piece.dataset.pin = '0';
  piece.dataset.label = clip.label;
  piece.dataset.slug = clip.slug;
  piece.dataset.src = clip.src;
  piece.style.width = `${piece.dataset.w}px`;
  piece.style.height = `${piece.dataset.h}px`;
  piece.style.zIndex = ++z;
  piece.innerHTML = `<div class="cut-wrap"><img src="${clip.src}" alt="${clip.label}" draggable="false"></div><span class="piece-caption">${clip.label}</span><span class="lock-badge" aria-hidden="true">LOCKED</span><span class="rotate-handle" data-rotate title="Drag to rotate">↻</span><span class="resize-handle nw" data-resize="nw"></span><span class="resize-handle ne" data-resize="ne"></span><span class="resize-handle sw" data-resize="sw"></span><span class="resize-handle se" data-resize="se"></span>`;
  piece.style.setProperty('--cut-poly', `polygon(${piece.dataset.cutPoly})`);
  piece.style.setProperty('--edge-poly', `polygon(${piece.dataset.edgePoly})`);
  piece.style.setProperty('--paper-tone', piece.dataset.paper);
  piece.style.setProperty('--edge-tone', piece.dataset.edge);
  piece.style.setProperty('--pad-top', `${piece.dataset.padTop}px`);
  piece.style.setProperty('--pad-right', `${piece.dataset.padRight}px`);
  piece.style.setProperty('--pad-bottom', `${piece.dataset.padBottom}px`);
  piece.style.setProperty('--pad-left', `${piece.dataset.padLeft}px`);
  piece.style.setProperty('--edge-x', `${piece.dataset.edgeOffsetX}px`);
  piece.style.setProperty('--edge-y', `${piece.dataset.edgeOffsetY}px`);
  piece.style.setProperty('--edge-rot', piece.dataset.edgeRot);
  return piece;
}

function addPiece(clip, sourceBtn){
  if(placedClipCount() >= LIMIT || sourceBtn.classList.contains('used')) return;
  pushHistory();
  const piece = buildImagePiece(clip);
  artboard.appendChild(piece);
  sourceBtn.classList.add('used');
  boardPieces.push(piece);
  applyTransform(piece);
  select(piece);
  wirePiece(piece);
  updateCount();
}

function addTextScrap(){
  const raw = prompt('Text scrap:', 'LOCAL ONLY');
  if(raw === null) return;
  const text = raw.trim().slice(0, 42);
  if(!text) return;
  pushHistory();
  const bw = artboard.clientWidth || 600;
  const bh = artboard.clientHeight || 600;
  const data = {
    text,
    x: String(Math.round(55 + Math.random() * Math.max(60, bw - 260))),
    y: String(Math.round(75 + Math.random() * Math.max(60, bh - 180))),
    w: String(Math.min(260, Math.max(160, text.length * 12 + 64))),
    h: '76',
    rot: String(randomPlacementRotation()),
    scale: '1',
    zIndex: String(++z)
  };
  const piece = createTextPieceFromData(data);
  boardPieces.push(piece);
  select(piece);
  updateCount();
}

function applyTransform(el){
  el.style.transform = `translate(${el.dataset.x}px, ${el.dataset.y}px) rotate(${el.dataset.rot}deg) scale(${el.dataset.scale})`;
}
function select(el){
  if(selected) selected.classList.remove('selected');
  selected = el;
  if(selected) selected.classList.add('selected');
  updateLockButton();
}

function updateLockButton(){
  const btn = document.getElementById('lockBtn');
  if(!btn) return;
  const locked = selected?.dataset.locked === '1';
  btn.textContent = locked ? 'Unlock' : 'Lock';
  btn.classList.toggle('active', locked);
  btn.disabled = !selected;
}

function toggleSelectedLock(){
  if(!selected) return;
  pushHistory();
  const locked = selected.dataset.locked === '1';
  selected.dataset.locked = locked ? '0' : '1';
  selected.classList.toggle('locked', !locked);
  activeDrag = null;
  updateLockButton();
}

function normalizeLayers(order = boardPieces){
  order.forEach((piece, i) => { piece.style.zIndex = String(10 + i); });
  z = 10 + order.length;
}

function moveSelectedLayer(where){
  if(!selected) return;
  const ordered = [...boardPieces].sort((a, b) => Number(a.style.zIndex || 0) - Number(b.style.zIndex || 0));
  const index = ordered.indexOf(selected);
  if(index < 0) return;
  const target = where === 'front' ? Math.min(ordered.length - 1, index + 1) : Math.max(0, index - 1);
  if(target === index) return;
  [ordered[index], ordered[target]] = [ordered[target], ordered[index]];
  normalizeLayers(ordered);
}
function wirePiece(piece){
  piece.addEventListener('pointerdown', (e) => {
    if(e.button && e.button !== 0) return;
    select(piece);
    if(piece.dataset.locked === '1'){
      activeDrag = null;
      e.preventDefault();
      return;
    }
    pushHistory();
    piece.setPointerCapture(e.pointerId);
    const rotateHandle = e.target.closest('[data-rotate]');
    if(rotateHandle){
      const rect = piece.getBoundingClientRect();
      activeDrag = {
        mode: 'rotate', el: piece,
        cx: rect.left + rect.width / 2,
        cy: rect.top + rect.height / 2,
        startAngle: Math.atan2(e.clientY - (rect.top + rect.height / 2), e.clientX - (rect.left + rect.width / 2)),
        rot: parseFloat(piece.dataset.rot || 0)
      };
      e.stopPropagation();
      return;
    }
    const handle = e.target.closest('[data-resize]');
    if(handle){
      const w = parseFloat(piece.dataset.w || piece.offsetWidth);
      const h = parseFloat(piece.dataset.h || piece.offsetHeight);
      activeDrag = {
        mode: 'resize', el: piece, handle: handle.dataset.resize,
        startX: e.clientX, startY: e.clientY,
        x: parseFloat(piece.dataset.x), y: parseFloat(piece.dataset.y),
        w, h, aspect: w / h
      };
      e.stopPropagation();
      return;
    }
    activeDrag = { mode: 'move', el: piece, startX: e.clientX, startY: e.clientY, x: parseFloat(piece.dataset.x), y: parseFloat(piece.dataset.y) };
  });
  piece.addEventListener('pointermove', (e) => {
    if(!activeDrag || activeDrag.el !== piece) return;
    if(activeDrag.mode === 'rotate'){
      const angle = Math.atan2(e.clientY - activeDrag.cy, e.clientX - activeDrag.cx);
      const delta = (angle - activeDrag.startAngle) * 180 / Math.PI;
      piece.dataset.rot = String(Math.round(activeDrag.rot + delta));
      applyTransform(piece);
      return;
    }
    if(activeDrag.mode === 'resize'){
      const dx = e.clientX - activeDrag.startX;
      const dy = e.clientY - activeDrag.startY;
      const east = activeDrag.handle.includes('e');
      const west = activeDrag.handle.includes('w');
      const south = activeDrag.handle.includes('s');
      const north = activeDrag.handle.includes('n');

      // Corner resize should scale proportionally, like a real pasted clipping.
      // Use the dominant drag axis so it feels natural and never distorts.
      const rawW = west ? activeDrag.w - dx : activeDrag.w + dx;
      const rawH = north ? activeDrag.h - dy : activeDrag.h + dy;
      const scaleFromW = rawW / activeDrag.w;
      const scaleFromH = rawH / activeDrag.h;
      let resizeScale = Math.abs(scaleFromW - 1) > Math.abs(scaleFromH - 1) ? scaleFromW : scaleFromH;
      resizeScale = Math.max(0.18, Math.min(5.25, resizeScale));

      const w = Math.round(activeDrag.w * resizeScale);
      const h = Math.round(activeDrag.h * resizeScale);
      let x = activeDrag.x;
      let y = activeDrag.y;
      if(west) x = activeDrag.x + (activeDrag.w - w);
      if(north) y = activeDrag.y + (activeDrag.h - h);

      piece.dataset.w = String(w);
      piece.dataset.h = String(h);
      // Allow oversized clippings to hang far off the pasteboard edge.
      piece.dataset.x = String(Math.max(-w + 40, Math.min(x, artboard.clientWidth - 40)));
      piece.dataset.y = String(Math.max(-h + 40, Math.min(y, artboard.clientHeight - 40)));
      piece.style.width = `${piece.dataset.w}px`;
      piece.style.height = `${piece.dataset.h}px`;
      applyTransform(piece);
      return;
    }
    piece.dataset.x = Math.max(-80, Math.min(activeDrag.x + e.clientX - activeDrag.startX, artboard.clientWidth - 60));
    piece.dataset.y = Math.max(-80, Math.min(activeDrag.y + e.clientY - activeDrag.startY, artboard.clientHeight - 60));
    applyTransform(piece);
  });
  piece.addEventListener('pointerup', () => activeDrag = null);
}
function changeSelected({rot=0, scale=0, front=false, back=false, del=false}){
  if(!selected) return;
  if(selected.dataset.locked === '1') return;
  pushHistory();
  if(del){
    const slug = selected.dataset.slug;
    [...tray.children].find(btn => btn.dataset.slug === slug)?.classList.remove('used');
    selected.remove();
    boardPieces = boardPieces.filter(p => p !== selected);
    selected = null;
    updateLockButton();
    updateCount();
    return;
  }
  selected.dataset.rot = parseFloat(selected.dataset.rot) + rot;
  selected.dataset.scale = Math.max(.18, Math.min(5.25, parseFloat(selected.dataset.scale) + scale)).toFixed(2);
  if(front) moveSelectedLayer('front');
  if(back) moveSelectedLayer('back');
  applyTransform(selected);
}

document.getElementById('rotateLeftBtn').onclick = () => changeSelected({rot:-7});
document.getElementById('rotateRightBtn').onclick = () => changeSelected({rot:7});
document.getElementById('biggerBtn').onclick = () => changeSelected({scale:.08});
document.getElementById('smallerBtn').onclick = () => changeSelected({scale:-.08});
document.getElementById('lockBtn').onclick = toggleSelectedLock;
document.getElementById('frontBtn').onclick = () => changeSelected({front:true});
document.getElementById('backBtn').onclick = () => changeSelected({back:true});
document.getElementById('deleteBtn').onclick = () => changeSelected({del:true});
document.getElementById('undoBtn').onclick = undoLast;
document.getElementById('clearBtn').onclick = clearBoard;
document.getElementById('newPackBtn').onclick = async () => { clearBoard(); await renderTray(); };
document.getElementById('shuffleTrayBtn').onclick = shuffleTray;
updateLockButton();

function clearBoard(){
  if(!boardPieces.length) return;
  pushHistory();
  boardPieces.forEach(p => p.remove());
  boardPieces = [];
  selected = null;
  updateLockButton();
  tray.querySelectorAll('.clip').forEach(btn => {btn.classList.remove('used'); btn.disabled = false;});
  updateCount();
}

document.addEventListener('keydown', (e) => {
  if((e.metaKey || e.ctrlKey) && e.key.toLowerCase() === 'z'){ undoLast(); return; }
  if(!selected) return;
  if(e.key.toLowerCase() === 'l') toggleSelectedLock();
  if(e.key === 'Delete' || e.key === 'Backspace') changeSelected({del:true});
  if(e.key === '[') changeSelected({rot:-7});
  if(e.key === ']') changeSelected({rot:7});
  if(e.key === '=' || e.key === '+') changeSelected({scale:.08});
  if(e.key === '-') changeSelected({scale:-.08});
});

async function loadImage(src){
  return new Promise((resolve,reject) => { const img = new Image(); img.onload = () => resolve(img); img.onerror = reject; img.src = src; });
}
async function exportPNG(){
  const rect = artboard.getBoundingClientRect();
  const size = 1400;
  const canvas = document.createElement('canvas');
  canvas.width = size; canvas.height = size;
  const ctx = canvas.getContext('2d');
  const sx = size / rect.width;
  ctx.fillStyle = '#fff8e8'; ctx.fillRect(0,0,size,size);
  ctx.strokeStyle = 'rgba(18,16,13,.09)'; ctx.lineWidth = 1;
  for(let i=0;i<size;i+=28*sx){ ctx.beginPath(); ctx.moveTo(i,0); ctx.lineTo(i,size); ctx.stroke(); ctx.beginPath(); ctx.moveTo(0,i); ctx.lineTo(size,i); ctx.stroke(); }
  ctx.fillStyle = '#12100d'; ctx.fillRect(22,22,415,34); ctx.fillStyle = '#fff1d2'; ctx.font = '18px Menlo, monospace'; ctx.fillText(`CLIPPING CLUB / ${todayKey()}`, 34, 46);
  const orderedPieces = [...boardPieces].sort((a, b) => Number(a.style.zIndex || 0) - Number(b.style.zIndex || 0));
  for(const p of orderedPieces){ await drawPiece(ctx, p, sx); }
  drawWatermark(ctx, size);
  const filename = `clipping-club-${todayKey()}.png`;
  canvas.toBlob(async (blob) => {
    if(!blob) return;
    const file = new File([blob], filename, { type: 'image/png' });
    const canShareFile = navigator.canShare && navigator.canShare({ files: [file] });
    if(canShareFile){
      try {
        await navigator.share({ files: [file], title: 'Clipping Club', text: 'My Clipping Club artifact' });
        return;
      } catch (err) {
        // If the user cancels native share, fall through to preview/save.
      }
    }
    const url = URL.createObjectURL(blob);
    const popup = window.open(url, '_blank');
    if(!popup){
      const a = document.createElement('a');
      a.href = url;
      a.download = filename;
      a.target = '_blank';
      document.body.appendChild(a);
      a.click();
      a.remove();
    }
    setTimeout(() => URL.revokeObjectURL(url), 60000);
  }, 'image/png');
}

function makeDatasetCutPath(ctx, w, h, poly){
  const fallback = '4% 0%, 100% 3%, 97% 95%, 0% 100%';
  const pairs = [...(poly || fallback).matchAll(/([0-9.]+)%\s+([0-9.]+)%/g)]
    .map(m => [Number(m[1]) / 100 * w, Number(m[2]) / 100 * h]);
  ctx.beginPath();
  pairs.forEach(([x,y], i) => i ? ctx.lineTo(x, y) : ctx.moveTo(x, y));
  ctx.closePath();
}


async function drawPiece(ctx, p, sx){
  if(p.dataset.kind === 'text'){ drawTextPiece(ctx, p, sx); return; }
  await drawImagePiece(ctx, p, sx);
}

async function drawImagePiece(ctx, p, sx){
  const img = await loadImage(p.dataset.src);
  const x = parseFloat(p.dataset.x)*sx, y = parseFloat(p.dataset.y)*sx;
  const w = parseFloat(p.dataset.w || p.offsetWidth)*sx, h = parseFloat(p.dataset.h || p.offsetHeight)*sx;
  const scale = parseFloat(p.dataset.scale), rot = parseFloat(p.dataset.rot) * Math.PI / 180;
  ctx.save();
  ctx.translate(x + w/2, y + h/2); ctx.rotate(rot); ctx.scale(scale, scale); ctx.translate(-w/2, -h/2);
  const fit = Math.min(w / img.naturalWidth, h / img.naturalHeight);
  const drawW = img.naturalWidth * fit;
  const drawH = img.naturalHeight * fit;
  const drawX = (w - drawW) / 2;
  const drawY = (h - drawH) / 2;
  ctx.shadowColor = 'rgba(18,16,13,.58)'; ctx.shadowOffsetX = 7; ctx.shadowOffsetY = 8; ctx.shadowBlur = 0;
  ctx.drawImage(img, drawX, drawY, drawW, drawH);
  ctx.restore();
}

function drawTextPiece(ctx, p, sx){
  const x = parseFloat(p.dataset.x)*sx, y = parseFloat(p.dataset.y)*sx;
  const w = parseFloat(p.dataset.w || p.offsetWidth)*sx, h = parseFloat(p.dataset.h || p.offsetHeight)*sx;
  const scale = parseFloat(p.dataset.scale || 1), rot = parseFloat(p.dataset.rot || 0) * Math.PI / 180;
  ctx.save();
  ctx.translate(x + w/2, y + h/2); ctx.rotate(rot); ctx.scale(scale, scale); ctx.translate(-w/2, -h/2);
  ctx.shadowColor = 'rgba(18,16,13,.75)'; ctx.shadowOffsetX = 5; ctx.shadowOffsetY = 5; ctx.shadowBlur = 0;
  ctx.fillStyle = '#d7ff2f'; ctx.fillRect(0, 0, w, h);
  ctx.shadowColor = 'transparent';
  ctx.strokeStyle = '#12100d'; ctx.lineWidth = Math.max(2, 3*sx); ctx.strokeRect(0,0,w,h);
  ctx.fillStyle = '#12100d'; ctx.font = `900 ${Math.max(22, 34*sx)}px Arial Black, Impact, sans-serif`;
  ctx.textAlign = 'center'; ctx.textBaseline = 'middle';
  wrapText(ctx, (p.dataset.text || '').toUpperCase(), w/2, h/2, w - 22*sx, 34*sx);
  ctx.restore();
}

function wrapText(ctx, text, x, y, maxWidth, lineHeight){
  const words = text.split(/\s+/); const lines=[]; let line='';
  words.forEach(word => { const test = line ? `${line} ${word}` : word; if(ctx.measureText(test).width > maxWidth && line){ lines.push(line); line=word; } else line=test; });
  if(line) lines.push(line);
  const start = y - (lines.length - 1) * lineHeight / 2;
  lines.slice(0,3).forEach((line,i) => ctx.fillText(line, x, start + i*lineHeight));
}

function drawWatermark(ctx, size){
  const text = 'clippingclubsm.com  /  powered by @pulp.sm';
  ctx.save();
  ctx.font = '900 24px Menlo, monospace';
  const w = ctx.measureText(text).width + 42;
  ctx.fillStyle = 'rgba(18,16,13,.82)'; ctx.fillRect(size - w - 24, size - 58, w, 34);
  ctx.fillStyle = '#fff1d2'; ctx.fillText(text, size - w - 4, size - 34);
  ctx.restore();
}

document.getElementById('exportBtn').onclick = exportPNG;
renderTray();
